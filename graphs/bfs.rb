def bfs(graph, node)
  queue = [node]
  node.discovered = true

  until queue.empty?
    
  end
  # until nodes.empty?
  #   node = nodes.shift
  #
  #   return node if node == target_node
  #   nodes.concat(node.children)
  # end
  #
  # nil
end
